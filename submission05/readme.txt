This website is complete and
satisfies all specifications.

I used the phpmyadmin gui to create the table and
manually entered the data. I used a single table `my_Product` 
and used a query to get the DISTINCT catagories.